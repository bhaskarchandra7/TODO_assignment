# TODO_assignment
A To-Do List web application built using Python and Django. The project provides RESTful APIs for task management and HTML templates for UI rendering. Django ORM and generic viewsets are not used.
